Migrate my Python FastAPI in-memory CRUD task API to SQLite using sqlite3. Keep the five endpoints and their request/response behavior unchanged: GET /tasks, GET /tasks/{id}, POST /tasks, PUT /tasks/{id}, DELETE /tasks/{id}. Create tasks.db and a tasks table automatically if missing, with id INTEGER PRIMARY KEY AUTOINCREMENT, title TEXT, done INTEGER. Seed exactly three example tasks only when the table is empty. Preserve 200/201/204/400/404 behavior. Use parameterized SQL placeholders for all values. Do not change the API contract or invent extra fields. Explain your storage-layer changes.